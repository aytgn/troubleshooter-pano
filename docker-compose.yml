version: '3.8'
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: ${DB_SIFRE}
      POSTGRES_DB: appdb
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql # Tabloyu otomatik kurması için

  apache_php:
    build: ./php-apache
    volumes:
      - ./html:/var/www/html
    environment:
       APP_DB_PASS: ${DB_SIFRE} 
    depends_on:
      - db

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - apache_php

volumes:
  pgdata:
